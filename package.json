{
  "name": "fitsync-pro",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "@nestjs/cli": "^11.0.16"
  }
}