const fs = require('fs'); let c = fs.readFileSync('apps/api/prisma/schema.prisma', 'utf8'); let fixed = c.split('model diet_templates')[0] + 'model diet_templates {\n  id          String    @id @default(dbgenerated(\\x22gen_random_uuid()\x22\)) @db.Uuid\n  coach_id    String    @db.Uuid\n  name        String\n  description String?\n  content     Json\n  created_at  DateTime? @default(now()) @db.Timestamptz(6)\n  updated_at  DateTime? @updatedAt @db.Timestamptz(6)\n\n  coach       profiles @relation(fields: [coach_id], references: [id], onDelete: Cascade)\n}\n'; fs.writeFileSync('apps/api/prisma/schema.prisma', fixed, 'utf8');
